import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class CalculatorTest {

    @Test
    public void calculatorCanAdd(){

        Calculator add = new Calculator(1);
        assertEquals("Earth", planet.getName());
    }
}
